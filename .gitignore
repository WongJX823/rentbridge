.env
.env.*
*.env
credentials.json
*.pem
*.key
.credentials

# Local API keys
config/openai.php
config/google.php
appPassword.txt

# Test/tooling output (regenerated by running the suites)
.phpunit.cache/
.playwright-mcp/
playwright-report/
test-results/
node_modules/

# DB backup dumps (backups/backup_db.ps1) — never commit real data
backups/*.sql
