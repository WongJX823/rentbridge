# Private ownership documents (IC copies, geran, utility bills) — landlord,
# assigned agent, and admin only. Served exclusively through
# documents/property_doc.php, which checks that authorization; never as a
# plain static file.
Order deny,allow
Deny from all
